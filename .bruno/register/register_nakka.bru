meta {
  name: register_nakka
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/register
  body: json
  auth: none
}

body:json {
  {
    "email": "nakka@test.com",
    "username": "Nakka",
    "password": "Password123!password"
  }
}

script:pre-request {
  const timestamp = Date.now();
  bru.setVar("timestamp", timestamp);
}

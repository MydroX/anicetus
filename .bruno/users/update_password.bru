meta {
  name: update_password
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/v1/users/{{user_uuid}}/password
  body: json
  auth: none
}

body:json {
  {
    "password": "NewPassword123!password"
  }
}

vars:pre-request {
  user_uuid: user_replace_me
}

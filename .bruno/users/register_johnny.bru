meta {
  name: register_johnny
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/users/register
  body: json
  auth: none
}

body:json {
  {
    "email": "johnny@test.com",
    "username": "johnny",
    "password": "password123!password"
  }
}

script:pre-request {
  const timestamp = Date.now();
  bru.setVar("timestamp", timestamp);
}
